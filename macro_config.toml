version = 1

# Recoil trajectories are uploaded transactionally over RTCP at runtime.
# Keep the compiled macro table empty so an OTA trajectory is never combined
# with a stale built-in weapon profile.
always_mask = []
layers = []

[random]
seed = 0
